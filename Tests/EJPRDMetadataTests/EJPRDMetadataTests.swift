import XCTest
@testable import EJPRDMetadata

final class EJPRDMetadataTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EJPRDMetadata().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
